# video_text_gui

動画上にテキストを配置し、MP4に焼き込むデスクトップGUI。
Python 3.12+ / PySide6 / ffmpeg / ffprobe を使用する。

## 起動

fish / bash 共通:

```sh
python video_text_gui.py edit input.mp4
```

1. 右側に文字を入力する。改行・日本語・複数テキストに対応。
2. 映像上の文字をドラッグするか、横位置・縦位置を数値で指定する。
3. フォント、サイズ、文字色、縁取り、背景色・透明度を調整する。
4. 開始・終了時刻を秒で指定し、音声付きで再生して確認する。
5. 「設定を保存」で再編集用JSON、「MP4を書き出す」で動画を保存する。

サイズ・縁取りは出力動画のピクセル単位。横位置・縦位置は文字画像の中心位置を
動画全体の百分率で指定する。各行は中央揃え。自動折り返しは行わない。
画面外にはみ出た文字は切り取られ、GUIに注意が表示される。
後から追加したテキストが手前になる。
既存の字幕を選んで「複製」を押すと、文字・装飾・位置・表示時間をコピーして
一覧の末尾に追加する。複製した字幕が選択され、元の字幕とは独立して編集できる。
同じ位置・時間に重なるため、必要に応じて移動・時刻変更する。Undoにも対応。

カラーピッカーで色・透明度を変更すると、その場で映像にプレビューする。
OKで確定し、キャンセルで元に戻る。確定した変更はUndo 1回で取り消せる。
背景の透明度は右側のスライダーでも調整できる（0%：不透明、100%：完全透明）。
ドラッグ中もプレビューに反映し、1回のドラッグをUndo 1回で取り消せる。
カラー絵文字にも対応する（Noto Color Emojiなど、対応フォントが必要）。
絵文字はフォント本来の色で表示し、文字色・縁取りは通常の文字に適用する。

表示区間は **開始時刻を含み、終了時刻を含まない**。
選択中の文字が現在の再生位置で非表示の場合、右側にその旨を表示する。
Ctrl+S で設定保存。未保存の変更がある場合は、終了・設定読み込み時に確認する。
Spaceで再生・一時停止を切り替える。文字・数値・フォントの入力中は入力操作を
優先する。映像をクリックすると、入力欄からフォーカスを移してSpaceを使える。

### Undo / Redo

- **Ctrl+Z** または「元に戻す」：直前の編集を取り消す。
- **Ctrl+Shift+Z / Ctrl+Y** または「やり直す」：取り消した編集を再適用する。
- 文字の追加・削除、入力、位置、フォント、サイズ、色、表示時間に対応。
  文字入力欄や数値入力欄にフォーカスがある場合も同じ履歴を使う。
- 1回のドラッグは1操作。同じ項目への0.8秒以内の連続入力もまとめる。
- 最大100操作。保存後も履歴を保持し、保存時点に戻ると未保存状態を解除する。
  設定を読み込むと履歴をリセットする。動画の書き出しはUndo対象外。

## 保存先

既定では、起動した作業ディレクトリに保存する。

| 用途 | ファイル |
| --- | --- |
| 再編集用設定 | `input.text.json` |
| 書き出し候補 | `input_text.mp4` |

既定の設定ファイルが存在すれば起動時に読み込む。
設定は動画の絶対パスと表示サイズを保持し、別の動画への誤適用を防ぐ。
`--settings` と `-o` でパスを変更できる。

fish / bash 共通:

```sh
python video_text_gui.py edit input.mp4 --settings layout.json -o captioned.mp4
python video_text_gui.py encode input.mp4 --settings layout.json -o captioned.mp4
python video_text_gui.py encode input.mp4 --settings layout.json -o captioned.mp4 -n
```

`encode` はGUIを表示せず保存済み設定から書き出す。
`-n` / `--dry_run` / `--dry-run` は、実行用コマンドを表示するだけで、
動画の読み込み・一時ファイル作成・外部コマンド実行を行わない。
ログに表示するコマンドは fish / bash 両方に貼り付けられる。

## 書き出し

- 回転情報を反映して合成し、縦動画を正しい向きで保存する。
- 文字はQtで透明画像に描画し、同じ画像・座標をプレビューとFFmpeg合成に使用する。
- H.264 / CRF 18 / yuv420p のMP4。映像を再エンコードする。
- フレーム時刻を保持する。奇数の幅・高さは右端・下端を1px拡張する。
- 音声は再エンコードせずコピーする。MP4に格納できない音声形式はエラーになる。
- 開始時点の編集内容で書き出す。進捗表示と中止ボタンがある。
- 完成するまで出力先と同じディレクトリの一時領域を使う。
  中止・エラー時には一時ファイルを削除し、既存ファイルや元動画を上書きしない。

## 検証

fish / bash 共通:

```sh
pytest -q --doctest-modules .
```

テストは日本語・縁取り、設定保存、不正値、上書き防止、および生成した回転付き
可変フレームレート動画で、表示区間・描画位置・フレーム時刻・音声の保持を確認する。
